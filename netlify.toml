[build]
command = "npm run build"
publish = "dist"
# Run cache warm-up automatically after successful deployment
onSuccess = "npm run warm-cache"

[build.environment]
NODE_VERSION = "20.19.0"
# Optional: Override default limit (defaults to 40)
# CACHE_WARM_LIMIT = "40"
