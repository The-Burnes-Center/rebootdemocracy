[build]
command = "npm run build"
publish = ".output/public"

[build.environment]
NODE_VERSION = "20.19.0"

# Catch-all redirect for ISR pages
# Static files are served first, so this only applies to non-static routes
[[redirects]]
from = "/*"
to = "/.netlify/functions/server"
status = 200
force = false
