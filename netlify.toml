[build]
command = "npm run build"
publish = "dist"

[build.environment]
NODE_VERSION = "20.19.0"

# Note: Deploy notifications are configured in Netlify Dashboard
# Site Settings → Build & Deploy → Deploy Notifications
# Add notification: POST https://your-site.netlify.app/api/warm-up
# Event: "Deploy succeeded"
# See WARM_UP_SETUP.md for detailed instructions
