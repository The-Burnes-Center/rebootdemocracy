[build]
command = "npm run build"
publish = ".output/public"

[build.environment]
NODE_VERSION = "20.19.0"

# Nitro's Netlify preset automatically handles routing for ISR pages
# No explicit redirects needed - Nitro generates the necessary routing
