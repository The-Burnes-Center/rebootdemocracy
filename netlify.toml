[build]
command = "npm run build"
publish = ".output/public"

[build.environment]
NODE_VERSION = "20.19.0"

# Nitro's netlify preset automatically handles routing
# No explicit redirects needed - Nitro generates the necessary routing
